{
  "name": "pwinty3.0",
  "version": "1.0.0",
  "author": "Ariel Weingarten",
  "description": "JS wrapper for the Pwinty 3.0 API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "yarn build:watch",
    "prepublish": "tsc",
    "build": "rm -rf dist && tsc",
    "build:watch": "rm -rf dist && tsc -w",
    "test": "jest",
    "clean": "rm -rf node_modules dist"
  },
  "dependencies": {
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "@types/node": "^13.11.0",
    "@types/node-fetch": "^2.5.5",
    "jest": "^25.2.7",
    "nock": "^12.0.3",
    "ts-jest": "^25.3.1",
    "typescript": "^3.8.3"
  }
}
